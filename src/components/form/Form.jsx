import React from "react";
import "./form.scss";


const Form = () => {
  const handleLink = (link) => window.open(link);

  return (
    <div className="form">
      
    </div>
  );
};

export default Form;
